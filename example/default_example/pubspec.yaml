name: example
description: A new Flutter project to quickly test flutter_clean_arch_setup.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  flutter_clean_arch_setup:
    path: ../..
flutter: